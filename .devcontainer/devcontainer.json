{
  "name": "EventQR Custom PHP Container",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "forwardPorts": [80],
  "postCreateCommand": "php -v && php -i | grep pdo_mysql"
}
